temporal_segmentation<-function(w,motion_features,patient_clinical_data,orgType){
  # w is the segment length in minutes
  # orgType indicates whether the features are organized TOD or TFR
  
  if (orgType == "tod"){
    
  } else if (orgType == "tfr") {
    
  } else {
    stop("data organization type is not compatible")
  }
  
  
}